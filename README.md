### manage_app script for local use

After first cloning/pulling this script, convert it into an executable by running:

```bash
chmod +x manage_app.sh
```

To start the app, run:

```bash
./manage_app.sh start
```

To terminate the app, run:

```bash
./manage_app.sh stop
```
